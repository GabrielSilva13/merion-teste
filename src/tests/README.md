# Tests

Helpers, mocks, fixtures e utilitários compartilhados para testes.

**Responsabilidade**: Setup de testes, factories, builders, mocks.

**Regra**: Apenas usado em arquivos `.test.ts` ou `.spec.ts`.
