# Application

Casos de uso e orquestração de lógica de negócio.

**Responsabilidade**: Use cases, serviços de aplicação, interfaces de repositórios.

**Exemplos**: SpinUseCase, CalculateWinsUseCase, UpdateBalanceUseCase.

**Regra**: Pode importar de `core/` e `domain/`.
