# Infrastructure

Implementações concretas de serviços externos e persistência.

**Responsabilidade**: Repositórios, APIs, storage, serviços de terceiros.

**Exemplos**: LocalStorageRepository, RandomNumberGenerator, SoundService.

**Regra**: Pode importar de `core/`, `domain/` e `application/`.
